#include <stdio.h>
#include <math.h>

 //function declaration
       int isEven(int num1);
       
      int main() {
      
      int num1;
      int result;
      
      num1 = 0;
      
      //run program if eof value is not called
      while(num1 != EOF){
        //ask for num1
        printf("enter number: ");
        scanf("%d", &num1);
        
        //if eof value is input, end program
        if(num1 == EOF){
            printf("program ended!");
             break;
        }
            
    //calling function
     result = isEven(num1);
     printf("result is: %d\n\n", result);
   
        }
        
         return 0;
    
    }
    
   //function to determine if numbers are even or odd
      int isEven(int num1){
          
          //declaration
          
         int ret1;
         
          if((num1 % 2) == 0)
          ret1 = 1;
          else
          ret1 = 0;
          
        return ret1;
    
    }
