#include <stdio.h>
#include <math.h>

 //function declaration
       int isEven(int num1, int num2, int num3, int num4, int num5);
       
      int main() {
      
      int num1, num2, num3, num4, num5;
      int result;
     
        //ask for numbers
        printf("enter any 5 integers(differentiate with a space): ");
        scanf("%d %d %d %d %d", &num1, &num2, &num3, &num4, &num5);

    //calling function
    result = isEven(num1, num2, num3, num4, num5);
    
     return 0;
    
    }
    
   //function to determine if numbers are even or odd
      int isEven(int num1, int num2, int num3, int num4, int num5){
          
          //declaration
          
         int ret1, ret2, ret3, ret4, ret5;
         int ret[5] = {ret1, ret2, ret3, ret4, ret5};
          
          if((num1 % 2) == 0)
          ret1 = 1;
          else
          ret1 = 0;
          
          if((num2 % 2) == 0)
          ret2 = 1;
          else
          ret2 = 0;
          
          if((num3 % 2) == 0)
          ret3 = 1;
          else
          ret3 = 0;
          
          if((num4 % 2) == 0)
          ret4 = 1;
          else
          ret4 = 0;
         
          if((num5 % 2) == 0)
          ret5 = 1;
          else
          ret5 = 0;
         
         printf("result is: %d %d %d %d %d", ret1, ret2, ret3, ret4, ret5);
        return;
    
    }
